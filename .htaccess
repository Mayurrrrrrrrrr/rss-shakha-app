# ============================================================
# RSS Shakha App — Production .htaccess
# ============================================================

Options -Indexes
Options -MultiViews

# ── Compression ─────────────────────────────────────────────
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/css application/javascript text/plain text/xml application/json image/svg+xml
</IfModule>

# ── Browser caching ─────────────────────────────────────────
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css                "access plus 0 seconds"
    ExpiresByType application/javascript  "access plus 0 seconds"
    ExpiresByType image/png               "access plus 1 month"
    ExpiresByType image/jpeg              "access plus 1 month"
    ExpiresByType image/gif               "access plus 1 month"
    ExpiresByType image/svg+xml           "access plus 1 month"
    ExpiresByType image/webp              "access plus 1 month"
    ExpiresByType image/x-icon            "access plus 1 year"
    ExpiresByType text/html               "access plus 0 seconds"
</IfModule>

# ── Security headers ────────────────────────────────────────
<IfModule mod_headers.c>
    Header always set X-Frame-Options            "DENY"
    Header always set X-Content-Type-Options     "nosniff"
    Header always set X-XSS-Protection           "1; mode=block"
    Header always set Referrer-Policy            "strict-origin-when-cross-origin"
    Header always set Permissions-Policy         "geolocation=(), camera=(), microphone=()"

    # Updated CSP to allow CDNs and Images for Ekatmata Stotra and Snapshot Sharing
    Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' cdn.tailwindcss.com unpkg.com cdn.jsdelivr.net translate.google.com translate.googleapis.com static.cloudflareinsights.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com unpkg.com translate.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: images.unsplash.com via.placeholder.com translate.googleapis.com; frame-src 'self' translate.google.com;"

    AddDefaultCharset UTF-8

    Header unset X-Powered-By

    <FilesMatch "\.(php|html)$">
        Header set Cache-Control "no-store, no-cache, must-revalidate"
    </FilesMatch>
    <FilesMatch "\.(css|js|png|jpg|jpeg|gif|svg|webp|ico|woff|woff2)$">
        Header set Cache-Control "no-store, no-cache, must-revalidate"
    </FilesMatch>
</IfModule>

# ── Protect sensitive files ──────────────────────────────────
<FilesMatch "(^\.env|\.env\..*|composer\.lock|composer\.json|package\.json|package-lock\.json)$">
    Require all denied
</FilesMatch>

<FilesMatch "\.(bak|backup|old|orig|save|swp|tmp|log)$">
    Require all denied
</FilesMatch>

<FilesMatch "^\.(?!htaccess)">
    Require all denied
</FilesMatch>

# ── Block direct folder access ───────────────────────────────
<IfModule mod_rewrite.c>
    RewriteEngine On

    # Public homepage: root goes to home.php
    RewriteRule ^$ /home.php [L]

    # Allow public content directories (standalone)
    RewriteRule ^ekatmata-stotra/ - [L]
    RewriteRule ^utsav/ - [L]
    RewriteRule ^content/ - [L]

    # Content Router: old folder URLs → content/index.php
    RewriteRule ^prarthna/?$        content/index.php?page=prarthna [L,QSA]
    RewriteRule ^subhashit/?$       content/index.php?page=subhashit [L,QSA]
    RewriteRule ^geet/?$            content/index.php?page=geet [L,QSA]
    RewriteRule ^ghoshnayein/?$     content/index.php?page=ghoshnayein [L,QSA]
    RewriteRule ^amrit-vachan/?$    content/index.php?page=amrit-vachan [L,QSA]
    RewriteRule ^ekatmata-mantra/?$ content/index.php?page=ekatmata-mantra [L,QSA]

    # Block sensitive directories
    RewriteRule ^config/         - [F,L]
    RewriteRule ^includes/       - [F,L]
    RewriteRule ^scripts/        - [F,L]
</IfModule>

# ── Error pages ─────────────────────────────────────────────
ErrorDocument 403 /home.php
ErrorDocument 404 /home.php
ErrorDocument 500 /home.php

# ── PHP hardening ────────────────────────────────────────────
<IfModule mod_php.c>
    php_flag display_errors           Off
    php_flag log_errors               On
    php_flag expose_php               Off
    php_value session.cookie_httponly 1
    php_value session.cookie_samesite Strict
    php_flag  session.use_strict_mode 1
</IfModule>
